# Test Configuration - Use MySQL but disable Flyway (DB already exists)
spring.datasource.url=jdbc:mysql://localhost:3306/streamlabDB?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=040626xw
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Disable Flyway for tests (DB already migrated)
spring.flyway.enabled=false

# Disable JPA schema validation
spring.jpa.hibernate.ddl-auto=none

# Disable Kafka (avoid connection errors)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# Mock external services credentials
OSS_ACCESS_KEY_ID=test
OSS_ACCESS_KEY_SECRET=test
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
KAFKA_BOOTSTRAP_SERVERS=
JWT_SECRET=test-secret-key-for-testing-purposes-only
