-- ============================================================
-- Redis 初始化脚本 — StreamLab
-- ============================================================
-- 使用方法: redis-cli < init-redis.sql
-- 或在 Redis Insight / RDM 中执行
-- ============================================================

-- ============================================================
-- 1. 点赞去重 Set（用户维度）
-- key = like:user:{userId}     value = Set<videoId>
-- ============================================================

-- 示例：用户 1 点赞了视频 100 和 200
-- SADD like:user:1 100
-- SADD like:user:1 200

-- TTL：用户点赞数据长期有效，设置 90 天
-- EXPIRE like:user:1 7776000

-- ============================================================
-- 2. 视频统计计数器
-- key = video:views:{videoId}    value = int
-- key = video:likes:{videoId}    value = int
-- key = video:danmaku:{videoId}  value = int
-- ============================================================

-- 示例：视频 100 的统计
-- SET video:views:100 0
-- SET video:likes:100 0
-- SET video:danmaku:100 0

-- TTL：30 天自动过期（由应用层刷新）
-- EXPIRE video:views:100 2592000
-- EXPIRE video:likes:100 2592000
-- EXPIRE video:danmaku:100 2592000

-- ============================================================
-- 3. 热门视频排行 ZSet
-- key = rank:videos:views        score = viewsCount
-- ============================================================

-- ZADD rank:videos:views 0 "video:100"
-- ZADD rank:videos:views 0 "video:200"

-- ============================================================
-- 4. 评论点赞计数器
-- key = comment:likes:{commentId}  value = int
-- ============================================================

-- SET comment:likes:12345 0
-- EXPIRE comment:likes:12345 2592000

-- ============================================================
-- 5. 分布式锁前缀
-- key = lock:{resource}           value = UUID
-- ============================================================

-- 由 RedisLockService 自动管理，无需手动创建

-- ============================================================
-- 6. 播放进度缓存
-- key = progress:user:{userId}:video:{videoId}  value = seconds
-- ============================================================

-- SET progress:user:1:video:100 120
-- EXPIRE progress:user:1:video:100 86400

-- ============================================================
-- 7. 通知未读计数
-- key = notification:unread:{userId}  value = int
-- ============================================================

-- SET notification:unread:1 0

-- ============================================================
-- 批量初始化脚本（按需执行）
-- ============================================================

-- 批量设置视频统计计数器（用于修复统计数据）
-- MSET video:views:1 0 video:likes:1 0 video:danmaku:1 0

-- 清理所有统计（谨慎使用！）
-- KEYS "video:views:*"
-- KEYS "video:likes:*"
-- KEYS "video:danmaku:*"
-- DEL <keys...>
